{
  "name": "oee-be",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "postinstall": "prisma generate",
    "dev": "npm run prisma:generate && npm run prisma:push && nodemon src/server.js",
    "start": "npm run prisma:generate && npm run prisma:push && node src/server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.1.4",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "prisma": "^5.0.0"
  }
}
